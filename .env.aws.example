# AWS X-Ray Tracing Configuration
# For use with AWS X-Ray and AWS Distro for OpenTelemetry

# AWS X-Ray daemon endpoint (if running locally) 
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# AWS region (required for X-Ray)
AWS_REGION=us-east-1

# X-Ray specific headers
OTEL_EXPORTER_OTLP_HEADERS={"x-amzn-trace-id": "Root=1-5e2b3c4d-5f6a7b8c9d0e1f2a3b4c5d6e;Parent=7f8a9b0c1d2e3f4a;Sampled=1"}

# Service configuration
OTEL_SERVICE_NAME=mapbox-mcp-server
OTEL_SERVICE_VERSION=0.0.1
OTEL_RESOURCE_ATTRIBUTES=service.name=mapbox-mcp-server,service.version=0.0.1,aws.region=us-east-1

# IAM role or credentials should be configured separately
# Either through IAM role, AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY, or AWS CLI profile
