# AWS Tracing Configuration Example
# Copy to .env.local and configure for your environment

# Basic configuration
MAPBOX_ACCESS_TOKEN=your_mapbox_token_here
NODE_ENV=production

# === AWS X-Ray Configuration ===
# Use AWS X-Ray for distributed tracing
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp.us-east-1.amazonaws.com:4318
AWS_REGION=us-east-1

# Optional: Custom service name and version
OTEL_SERVICE_NAME=mapbox-mcp-server
OTEL_SERVICE_VERSION=0.5.4

# === AWS CloudWatch Configuration ===
# Alternative: Use CloudWatch for traces
# OTEL_EXPORTER_OTLP_ENDPOINT=https://otel-ingest.us-east-1.amazonaws.com/traces

# === Prometheus for Amazon Managed Prometheus ===
# Enable Prometheus metrics for AMP
OTEL_ENABLE_PROMETHEUS=true
OTEL_PROMETHEUS_PORT=9090

# === Development/Debug ===
# Uncomment for local debugging with console output
# OTEL_ENABLE_CONSOLE_EXPORTER=true

# === AWS Credentials ===
# Configure via one of these methods:
# 1. IAM Role (recommended for ECS/Lambda/EC2)
# 2. Environment variables:
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# 3. AWS CLI profile:
# AWS_PROFILE=your_profile_name

# === Sampling Configuration ===
# Reduce trace volume in high-traffic environments
# OTEL_TRACES_SAMPLER=traceidratio
# OTEL_TRACES_SAMPLER_ARG=0.1  # Sample 10% of traces
