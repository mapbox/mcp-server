# OpenTelemetry Tracing Configuration Examples

# OpenTelemetry Tracing Configuration Examples

# ⚠️ Console tracing should be avoided with stdio transport
# Console output interferes with MCP's stdio JSON-RPC communication
# Safe to use with SSE transport (SERVER_TRANSPORT=sse)

# Console tracing for development (SSE transport only)
# OTEL_EXPORTER_CONSOLE_ENABLED=true

# OTLP HTTP endpoint for production (recommended for all transports)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Optional: OTLP headers for authentication
# OTEL_EXPORTER_OTLP_HEADERS={"Authorization": "Bearer your-token-here"}

# Standard OpenTelemetry environment variables
OTEL_SERVICE_NAME=mapbox-mcp-server
OTEL_SERVICE_VERSION=0.0.1
OTEL_RESOURCE_ATTRIBUTES=service.name=mapbox-mcp-server,service.version=0.0.1

# Sample rate (0.0 to 1.0) - useful for high-volume production environments
# OTEL_TRACES_SAMPLER=traceidratio
# OTEL_TRACES_SAMPLER_ARG=0.1
